#include<iostream>
using namespace std;

int main()
{
    int n,i,find,index=0;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++)
        cin>>a[i];
    cin>>search;
    for(i=0;i<n;i++ && index++)
        if (a[i]==find) && (index<=n);
        {
            break;
        }
    cout<<index;
    return 0;
}