#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    int t,i;
    cin>>t;
    while(t--)
    {
        char str[50];
        gets(str);
        puts(str);
    }
    return 0;
}